/**
 * core/smoothing/index.ts
 * ---------------------------------------------------------------------------
 * Barrel export for the smoothing layer. Like `core/math`, this is pure
 * TypeScript with no framework dependency — portable to a future Flutter
 * port alongside the math layer it builds on.
 * ---------------------------------------------------------------------------
 */

export * from './OneEuroFilter';
export * from './QuaternionSmoother';
